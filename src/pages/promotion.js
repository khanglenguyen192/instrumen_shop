import React from "react";

const Promotion = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "90vh",
      }}
    >
      <h1>Promotion</h1>
    </div>
  );
};

export default Promotion;
